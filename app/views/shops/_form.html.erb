<%= simple_form_for(@shop, :url => shop_path, :html => {:class => 'form-horizontal' }) do |f| %>
<%= f.input :login %>
<%= f.input :password %>

<%= f.input :automatic_fulfillment %>

<div class="form-actions"><%= f.button :submit, :class => "btn btn-primary" %></div>

<% end %>